import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class MainSpec extends AnyFlatSpec with Matchers {

  "Main" should "assign and deliver packages correctly" in {
    // You can call the main function and check if the output is as expected
    // Here you might want to capture stdout to verify the print statements
  }
}
